# Update the core pip tools first
python -m pip install --upgrade pip setuptools wheel

# Install the "heavy" packages from binary wheels first
pip install --only-binary :all: Pillow==11.0.0 numpy==2.1.3 pandas==2.2.3 psycopg2-binary==2.9.10 mysql-connector-python==9.1.0

# Install the rest of the list
pip install -r safe_requirements.txt --prefer-binary